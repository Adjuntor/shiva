services:
  shiva:
    image: ghcr.io/adjuntor/shiva/shiva:1.0.0
    container_name: shiva
    network_mode: bridge
    restart: unless-stopped
    volumes:
        - <volume-name>:/usr/src/app/config
